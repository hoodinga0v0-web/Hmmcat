# 흠냥이 오디오 복구 검증 기록

검증일: 2026-09-09

## 수정 내용

사용자가 Android Chrome에서 탭·앱 전환 후 소리가 나지 않는 현상을 보고했다. 기존 코드에는 화면 복귀 시 음원을 다시 준비하는 처리가 없었으며 Aha·야옹 재생 실패는 무시하고 있었다. 이것이 실제 휴대폰에서 발생한 문제의 유일한 원인인지까지 확인한 것은 아니다.

- 화면을 벗어나면 음원·이벤트 음향과 진행 중인 재생 요청을 정리한다. 누르던 상태도 취소해 복귀 후 긴 누르기가 잘못 집계되지 않게 한다.
- 복귀 후 첫 터치나 Enter·Space 입력 안에서 음원을 다시 로드하고 중단된 오디오 컨텍스트를 재개한다. 복귀만으로 소리를 재생하지 않는다.
- 재생 거부 또는 600ms 이상 시작되지 않는 상태를 감지하면 한 번 다시 로드한다. 계속 실패하면 같은 내장 음원을 Web Audio로 재생한다.
- 오디오 컨텍스트를 재사용하며 오래된 비동기 재생 요청이 화면 복귀·음소거 후 갑자기 소리를 내지 않도록 한다.
- 흠·긴 흐음·Aha·야옹 원본 음원, 음소거 설정, 이벤트 음향 구성과 저장 기록을 유지한다.

## 브라우저별 결과

| 환경 | 결과 | 확인 범위와 제한 |
|---|---|---|
| Windows Chrome 152.0.7977.82 | 통과 | 아래 복구 시나리오 및 실제 브라우저 페이지 동결·재개 |
| Chrome의 Pixel 7 모사 환경 | 통과 | 모바일 입력·화면 설정에서 동일 시나리오. Android 운영체제나 실제 기기 시험은 아님 |
| Windows Edge 152.0.4191.66 | 통과 | 아래 복구 시나리오 및 실제 브라우저 페이지 동결·재개 |
| Firefox 153.0 자동화 런타임 | 판정 불가 | 이 Windows 환경에서 mozglue 어셈블리 관련 실행 오류로 브라우저를 시작하지 못함 |
| WebKit 26.5 Windows 자동화 런타임 | 판정 불가 | AudioContext를 제공하지 않고 내장 MP3도 재생되지 않아 복구 전후 비교 불가. 실제 iPhone Safari의 결과로 해석할 수 없음 |
| 실제 Android Chrome·iPhone Safari | 미검증 | 연결된 휴대폰이 없어 기기에서 앱 전환·오디오 출력까지 직접 확인하지 못함 |

Chrome과 Edge는 같은 Chromium 계열이다. 이번 결과로 다른 모든 브라우저 엔진이나 휴대폰에서 문제가 해결됐다고 단정하지 않는다. Safari도 탭·앱 전환 때 오디오 컨텍스트가 중단될 수 있다는 것은 [MDN의 오디오 상태 설명](https://developer.mozilla.org/en-US/docs/Web/API/BaseAudioContext/state)에 명시되어 있다. 따라서 중단 상태를 재개하는 처리는 포함했지만, 이 게임의 Safari 실기기 재현 여부는 확인하지 못했다.

## 시험 방법

작업용 HTML 사본에만 재생 관찰·실패 주입 코드를 추가했다. 배포 파일에는 테스트 훅이 없다.

1. 기존 버전에서 중단된 미디어 재생 상태를 주입했다. 화면 복귀 후 Aha 재생 시간이 0에 머무르는 것을 확인했다. 이는 원인 후보를 재현한 통제 실험이며 Android 운영체제 버그를 직접 재현한 결과는 아니다.
2. 수정 버전에서 포커스 전환, 문서 숨김·표시, pagehide·pageshow, freeze·resume 이벤트를 각각 전달했다. 각 조건에서 흠·긴 흐음·Aha·야옹 네 음원의 재생 시간이 다시 진행하는지 확인했다.
3. play()가 끝없이 대기하는 경우, 재생을 거부하는 경우, 미디어 요소가 계속 실패하는 경우를 시험했다. 같은 Aha 음원을 디코딩한 대체 재생도 확인했다.
4. 실제 오디오 컨텍스트를 suspend한 뒤 입력으로 running 상태가 되는지, 반복 실행에도 컨텍스트가 하나만 생성되는지 확인했다.
5. 음소거를 유지한 채 복귀하면 소리가 시작되지 않는지, 음소거 해제 후 재생되는지, 이전 재생 요청이 뒤늦게 실패해도 소리가 재시작되지 않는지 확인했다.
6. Chrome·Edge의 개발자 프로토콜로 페이지를 실제 frozen·active 상태로 바꾼 뒤 음원이 재생되는지 확인했다.
7. 주요 이벤트 24개의 정확한 횟수·보상·스킵 방지·종료, 백만 회 귀 엔딩과 네 고양이, 옷장 19종, 저장 이전, 언어 UI 및 초기화 검사를 통과했다.

음원 검사는 재생 시간과 Web Audio 노드·상태를 관찰한 자동 검사다. 실제 휴대폰 스피커에서 소리를 들은 청음 검사는 포함하지 않는다.

복구 방식은 [Chrome의 play() 중단·load() 안내](https://developer.chrome.com/blog/play-request-was-interrupted), [Chrome 페이지 생명주기 문서](https://developer.chrome.com/docs/web-platform/page-lifecycle-api), 위 MDN 오디오 상태 문서를 참고했다.

## 휴대폰에서 확인할 순서

1. 배포 사이트를 새로고침하고 음소거가 꺼진 상태에서 고양이를 누른다.
2. 다른 탭과 다른 앱으로 각각 이동했다 돌아와 첫 터치에서 흠 소리가 나는지 확인한다.
3. 화면을 잠갔다 돌아온 뒤에도 같은 방법으로 확인한다.
4. 음소거한 상태로 나갔다 돌아오면 계속 조용하고, 음소거를 해제하면 다시 소리가 나는지 확인한다.

이 순서는 기기 검증을 위한 안내이며 실행 완료로 집계하지 않았다.
